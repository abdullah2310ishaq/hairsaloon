import 'package:flutter/material.dart';

class EmployeeAgreementScreen extends StatelessWidget {
  const EmployeeAgreementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Employee Agreement'),
      ),
      body: const Center(
        child: Text('Employee agreement form placeholder'),
      ),
    );
  }
}

